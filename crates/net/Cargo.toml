[package]
name = "session_tracker_net"
version.workspace = true
license.workspace = true
edition = "2021"

[dependencies]
session_tracker_core = { path = "../core" }
ureq = { version = "3", features = ["json"] }
log = "0.4"

[dev-dependencies]
dotenvy = "0.15"
tempfile = "3"
